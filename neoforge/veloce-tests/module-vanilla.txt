# End-to-end craft test for the modules that ship with the mod itself, with no
# third-party mod installed: crafting table, furnace, brewing stand.
#
# This is the vanilla-only run: nothing else is on the classpath, so only these
# three modules exist. Each one gets a real network, a real recipe from the game,
# and is asked for the result the way a player asks for it.

/cv testmodule crafting
expect: building and waiting
expect: ingredients placed:

wait: 120

/cv testmodule result
expect: PASS

# The same for the furnace: it needs a heat source in the network, so it
# exercises a different availability path than the crafter.
/cv testmodule furnace
expect: building and waiting

wait: 120

/cv testmodule result
expect: PASS
