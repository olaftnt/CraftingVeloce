# Can the network make a CASING (create:item_application), and does it COUNT it?
#
# The recipe id is NESTED: Create keeps its recipes one folder deep, so it is
# create:item_application/andesite_casing_from_log and NOT create:andesite_casing_from_log.
# Writing it the short way makes findRecipe return null and the test blames the machine
# for a typo in the test.
#
# The verdict prints `terminalShows=` - the number computeCraftableCounts returns for the
# item, i.e. exactly the yellow "+N" the terminal draws. That is the measurement the "+N"
# bug needs; the kinetic machine is driven by the motor the rig places for it.
/cv testmodule create veloce_create_deployer_module "create:item_application/andesite_casing_from_log"
expect: building and waiting

wait: 120

/cv testmodule result
expect: PASS
