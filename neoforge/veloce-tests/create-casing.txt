# Can the network make a CASING (create:item_application), and does it COUNT it?
#
# The verdict prints `terminalShows=` - the number computeCraftableCounts returns for the
# item, i.e. exactly the yellow "+N" the terminal draws. 0 means "the network cannot make
# any more right now", which is a legitimate answer when the ingredients are missing; the
# point of the test is to see the number AT ALL.
#
# BLOCKED for now: /cv testmodule create only BUILDS the rig (with a rotation source
# placed for the kinetic machine) and skips, because the recipe is still selected in the
# same tick as the blocks and Create propagates rotation over the following ticks. Until
# the pick is deferred, this script reports SKIP - see the UNIMPLEMENTED comment.
/cv testmodule create veloce_create_deployer_module "create:andesite_casing_from_log"
expect: testing for create not implemented
